# Container Monitoring Stack — Quick Start

## Prerequisites
- Docker and Docker Compose installed
- Linux host (recommended) or Docker for Mac/Windows
- Enough privileges to mount `/proc`, `/sys` (for node_exporter/cadvisor)

## Quick start
1. Clone the repo

```bash
git clone <repo-url> container-monitoring
cd container-monitoring
